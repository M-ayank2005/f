/** @type {import('next').NextConfig} */
const nextConfig = {

    // next.config.js
    images: {
      domains: ['images.unsplash.com','ih0.redbubble.net'],
      
    },
    trailingSlash: true,
   
  
};

export default nextConfig;
